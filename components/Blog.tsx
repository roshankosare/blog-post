import Card from "./ui/Card";

interface BlogProps {}

const Blog: React.FC<BlogProps> = ({}) => {
    return (
        <Card className="w-full min-h-[300px]">
            blog
        </Card>
    )
}

export default Blog;
